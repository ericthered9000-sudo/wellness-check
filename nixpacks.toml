# Nixpacks configuration for HomeBeacon backend

[phases.install]
dependsOn = ["setup"]
cmds = ["npm install --workspace=backend"]
cacheDirs = ["node_modules"]

[phases.build]
dependsOn = ["install"]
cmds = ["npm run build --workspace=backend"]

[start]
cmd = "npm start --workspace=backend"
